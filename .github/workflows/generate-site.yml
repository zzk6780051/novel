name: Generate Novel Site

on:
  push:
    paths:
      - 'novel/**'
  workflow_dispatch:
  workflow_run:
    workflows: ["Split Novel Chapters"]
    types:
      - completed

jobs:
  generate-site:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
        
    - name: Generate HTML site
      run: |
        echo "开始生成小说站点..."
        python .github/scripts/generate_site.py
        echo "站点生成完成"
        
    - name: List generated files
      run: |
        echo "生成的站点结构:"
        find dist -type f -name "*.html" | head -20
        echo "..."
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Deploy to Cloudflare Pages
      uses: cloudflare/pages-action@1
      with:
        apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        projectName: novels-site
        directory: ./dist
        gitHubToken: ${{ secrets.GITHUB_TOKEN }}      with:
        apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        projectName: novels-site
        directory: ./dist
        gitHubToken: ${{ secrets.GITHUB_TOKEN }}
